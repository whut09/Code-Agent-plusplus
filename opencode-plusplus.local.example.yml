# Copy this file to opencode-plusplus.local.yml for local LLM usage.
# Keep opencode-plusplus.local.yml private. It is ignored by git.
llm:
  enabled: true
  provider: openai-compatible
  baseUrl: xx
  apiKey: xx
  model: xx
  temperature: 0.2
  maxTokens: 1200
